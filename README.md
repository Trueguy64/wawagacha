lalalala
npm run bot:deploy -w server   # register the slash commands (re-run when they change)
npm run bot:dev -w server      # run with reload
npm run bot -w server          # run the built bot (after npm run build)
npm run dev